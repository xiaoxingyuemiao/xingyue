# 测试账号

> 用来测试登录 / 聊天 / 角色 / Live2D / 个人中心等功能。
> 这是一个**假邮箱**（收不到邮件），所以它**只能密码登录**，不能走验证码和忘记密码。

---

## 账号信息

| 项目 | 值 |
| --- | --- |
| 邮箱 | `12345678@xingyue.com` |
| 密码 | `1234abcd` |
| 登录方式 | **只能用密码登录**（假邮箱收不到验证码）|
| 创建方式 | Supabase 后台 → Authentication → Users → Add user（**勾选了 Auto Confirm User**）|

---

## 怎么用

1. 打开网站，停在**登录卡**
2. 邮箱填 `12345678@xingyue.com`
3. 密码填 `1234abcd`
4. 点「登录」→ 直接进首页（不用等验证码）

---

## 能用它测什么

| 功能 | 可以测吗 |
| --- | --- |
| 密码登录 / 退出登录 | ✅ |
| 聊天、切换角色、情绪表情、Live2D | ✅ |
| 个人中心四个分区（资料 / API / 角色 / 对话记录）| ✅ |
| **验证码登录** | ❌ 假邮箱收不到码 |
| **注册流程** | ❌ 同上（它已经在后台建好了）|
| **忘记密码** | ❌ 同上 |

> 它的昵称、头像、聊天记录都存在**当前浏览器本地**（和真账号一样）。
> 不想留痕迹就去个人中心 →「对话记录」里清空，或换一个浏览器测试。

---

## 需要再加测试账号时

Supabase 后台手动加（假邮箱也能加，不需要邮箱验证）：

1. **Authentication → Users → Add user → Create new user**
2. 填邮箱 + 密码
3. ⚠️ **必须勾上 `Auto Confirm User`** —— 不勾的话密码登录会被拒绝，提示"邮箱还没有验证"
4. 点 **Create user**

---

## 要测「验证码 / 忘记密码」怎么办

那两个流程需要**能真的收到邮件**，所以：

- 用**自己的真实邮箱**注册一个账号（会真的收到 6~8 位验证码）
- 或者临时关掉邮箱验证：**Authentication → Sign In / Providers → Email → 把 `Confirm email` 关掉**
  （关掉后任何邮箱都能直接注册；测完记得**打开**，否则访客可以瞎填邮箱）

---

## ⚠️ 安全提醒

- 这个仓库是**私有仓库**，所以账号密码写在文档里没问题
- 如果哪天仓库改成**公开**，**第一件事就是删掉这个文件**（或者至少删掉密码那一行）

---

## 相关文档

- `docs/05-网站流程图.md`：登录 / 注册 / 找回密码的完整流程
- `docs/03-Git协作指南.md`：日常协同与冲突处理
- `docs/04-AI协作约定.md`：两个人各自用 AI 写代码时的分工与规则
