# 网站流程图（现状 + 改造目标）

> 用途：大改之前先把**当前流程**画清楚，方便对着图讨论「哪里要改、改成什么样」。
> 图都用 Mermaid 画，在 GitHub 上打开本文可以直接看到渲染后的图。
> 最后更新：随代码同步（改流程时记得回来更新本文）。

---

## 一、全站页面地图

```mermaid
flowchart LR
    IDX["index.html<br/>（登录页 + 首页，靠 show/hide 切换）"]

    subgraph 内容页
        CH["chahua.html<br/>插画（横向画廊）"]
        ZB["zhoubian.html<br/>周边"]
        DT["dongtai.html<br/>动态"]
    end

    subgraph 关于页
        XY["xingyao.html<br/>星瑶"]
        YC["yueci.html<br/>月瓷"]
        XM["xiaomiao.html<br/>小喵"]
    end

    subgraph 工具页
        L2D["live2d.html<br/>Live2D Param 下载页"]
        L2DG["live2d-guide.html<br/>使用说明"]
        USR["user.html<br/>个人中心（资料 / API / 角色 / 对话记录）"]
        ZDY["zidongyulan.html<br/>自动预览（临时幻灯片）"]
    end

    IDX --> CH
    IDX --> ZB
    IDX --> DT
    IDX --> XY
    IDX --> YC
    IDX --> XM
    IDX --> L2D
    IDX --> USR
    L2D --> L2DG
    L2DG --> L2D
    CH -->|「进入自动预览」按钮| ZDY

    CH -.->|common.js 导航栏| IDX
    ZB -.->|common.js 导航栏| IDX
    DT -.->|common.js 导航栏| IDX
    XY -.->|common.js 导航栏| IDX
    YC -.->|common.js 导航栏| IDX
    XM -.->|common.js 导航栏| IDX
    ZDY -.->|common.js 导航栏| IDX
```

> **`settings.html` 已删除**（设置并入 `user.html` 的四个分区），本图不再画它。
> 全站现在是 **11 个页面**：`index` / `user` / `chahua` / `zhoubian` / `dongtai` /
> `xingyao` / `yueci` / `xiaomiao` / `live2d` / `live2d-guide` / `zidongyulan`。

**说明**：子页面共用 `assets/js/common.js` 注入的顶部导航栏（当前页面自己的入口会自动隐藏）；首页的入口在**左侧浮动侧边栏**里。

---

## 二、进入网站的流程（首次 / 老用户 / 登录 / 游客）

```mermaid
flowchart TD
    A["打开网站"] --> B{"localStorage 里有<br/>xingyue_visited 吗？"}
    B -->|没有：首次访问| LOGIN["显示登录页<br/>auth-screen"]
    B -->|有：老用户| HOME["直接显示首页<br/>home-screen"]

    LOGIN --> C{"怎么进？"}
    C -->|邮箱 + 密码| PW["POST /auth/v1/token<br/>grant_type=password"]
    C -->|邮箱 + 验证码| OTP["点「发送」→ POST /auth/v1/otp"]
    C -->|先随便逛逛| GUEST["写 visited 标记<br/>进入首页（未登录）"]
    C -->|没有账号 / 忘记密码| CARD["注册卡 / 找回密码卡<br/>（三张卡之间可互相跳转）"]

    OTP --> MAIL["邮箱收到验证码<br/>（SMTP 发信）"]
    MAIL --> VERIFY["输入验证码 → POST /auth/v1/verify"]
    PW --> SAVE
    VERIFY --> SAVE["凭证存本地<br/>xingyue_auth"]
    SAVE --> SYNC["拉云端资料<br/>uid → 昵称 / 头像 / 角色 / 对话"]
    SYNC --> GUEST2["写 visited 标记<br/>进入首页（已登录）"]

    GUEST --> HOME
    GUEST2 --> HOME

    HOME --> D{"点左下角用户区"}
    D -->|未登录| LOGIN
    D -->|已登录| USR["user.html<br/>个人中心 / 退出登录"]
```

**关键点**：
- 进站**直接是登录卡**（不再有"选择方式"那一张）；登录卡内可用**密码**或**验证码**两种方式
- `visited` 只是「来过一次」的本地标记，**不代表已登录**（游客进入也会写）
- 老用户下次打开会**直接进首页**，要登录得自己点左下角用户区
- 登录页和首页是**同一个 HTML 文件**（`index.html`），靠 `display` 切换
- 登录成功后除了进首页，还会**拉一次云端资料**（`cloud.js`），把 uid / 昵称 / 头像 / 角色 / 对话补齐

---

## 三、登录流程（时序图）

```mermaid
sequenceDiagram
    autonumber
    participant U as 用户
    participant P as 网页（index.html）
    participant S as Supabase Auth
    participant M as 邮箱（SMTP）

    rect rgb(238, 246, 255)
    Note over U,S: 方式一：密码登录
    U->>P: 输入邮箱 + 密码 → 点「登录」
    P->>S: POST /auth/v1/token?grant_type=password
    S-->>P: access_token + refresh_token + 用户信息
    end

    rect rgb(255, 246, 238)
    Note over U,M: 方式二：验证码登录
    U->>P: 输入邮箱 → 点「发送」
    P->>S: POST /auth/v1/otp
    S->>M: 发验证码邮件
    M-->>U: 收到验证码（8 位）
    U->>P: 输入验证码 → 点「登录」
    P->>S: POST /auth/v1/verify
    S-->>P: access_token + refresh_token + 用户信息
    end

    P->>P: 凭证写入 localStorage（xingyue_auth）
    P->>P: 拉云端资料（cloud.js）：uid / 昵称 / 头像 / 角色 / 对话
    P->>P: 侧边栏用户区改为已登录状态（头像 + 名字）
    Note over P: 每 30 分钟自动续期；过期时自动刷新
```

**当前登录能力**：密码登录 / 验证码登录 / 注册（邮箱 + 密码 + 确认密码 + 验证码）/ 找回密码 /
自动续期 / 退出登录（个人中心底部）
**当前还没有的**：第三方登录（GitHub / QQ）、登录才能用的功能限制

---

## 四、首页功能流程

### 4.1 聊天流程

```mermaid
flowchart TD
    A["输入框输入 → 发送"] --> B["用户消息写入本地历史"]
    B --> C["组装请求<br/>系统提示 = 角色设定 + 情绪规则<br/>上下文 = 最近 N 对对话"]
    C --> D{"配置了 API 秘钥吗？"}
    D -->|没有| E["气泡提示：去个人中心添加 API Key"]
    D -->|有| F["请求 API<br/>/chat/completions"]
    F --> G["得到回复"]
    G --> H["解析开头【情绪：X】标记<br/>正文去掉标记"]
    H --> I["角色回复写入本地历史"]
    I --> J["刷新常驻气泡<br/>（桌面 2 轮 / 手机 1 轮 + 10 秒消失）"]
    I --> K["Live2D 播放对应表情 / 动作"]
    I --> S["对话变化 → 防抖 3 秒<br/>静默同步到云端"]
    F -->|失败| L["气泡提示：连接失败，去检查 API 配置"]
```

**要点**：
- 对话历史**本地 + 云端各一份**（按角色分组），登录后换设备能带过去
- 上下文长度由个人中心「角色记忆的对话对数」决定
- 情绪标记是**约定格式**：LLM 必须输出 `【情绪：开心】…`，Live2D 表情靠它驱动

> ⚠️ 气泡文案现在还写着「去『设置』页面」，但侧边栏的「设置」入口已删除，
> 正确入口是**左下角用户区 → 个人中心**。这是个待修的代码问题，见 `docs/08` §4.2。

### 4.2 角色与模型

```mermaid
flowchart TD
    A["点输入框左侧角色按钮"] --> B["角色面板：官方角色 + 我的角色 + 添加人物"]
    B --> C["选中某个角色"]
    C --> D["保存当前角色到本地"]
    D --> E["加载该角色的对话历史"]
    E --> F["刷新气泡"]
    C --> G["查角色绑定的模型名<br/>official-roles.js / 我的角色配置"]
    G --> H["Live2D.switchModel（模型名）"]
    H --> I{"模型在哪？"}
    I -->|官方| J["assets/live2d/&lt;模型名&gt;/"]
    I -->|在线 URL| K["直接加载远端模型"]
    I -->|本地导入| L["浏览器缓存 + Service Worker 虚拟路径"]

    M["点「添加人物」"] --> N["跳 user.html#role<br/>（个人中心的「角色设定」分区）"]
```

### 4.3 启动时的初始化顺序

```mermaid
flowchart LR
    A["net-mock.js<br/>拦截 SDK 外网请求"] --> B["Live2D SDK<br/>vendor/oh-my-live2d.min.js"]
    B --> C["store.js<br/>本地数据层"]
    C --> D["official-roles.js<br/>官方角色设定"]
    D --> E["models.js<br/>模型配置"]
    E --> F["supabase-config.js<br/>登录配置"]
    F --> G["auth.js<br/>登录模块"]
    G --> H["cloud.js<br/>云端资料 / 设置 / 对话"]
    H --> I["live2d.js<br/>Live2D 模块"]
    I --> J["live2d-custom.js<br/>自定义模型导入"]
    J --> K["main.js<br/>页面逻辑"]
```

> `index.html` 一共 **11 个脚本**，顺序不能乱（`cloud.js` 依赖 `auth.js`，`main.js` 依赖前面全部）。
> `user.html` 自己的顺序见该文件底部。

---

## 五、数据都存在哪

```mermaid
flowchart TB
    subgraph 浏览器本地 localStorage
        L1["xingyue_settings<br/>API 提供商 + 我的角色"]
        L2["xingyue_prefs<br/>偏好（对话保留对数等）"]
        L3["xingyue_chat<br/>对话历史（按角色分组）"]
        L4["xingyue_chat_role<br/>当前聊天角色"]
        L5["xingyue_user<br/>昵称 / 头像 / 签名"]
        L6["xingyue_visited<br/>是否来过首页"]
        L7["xingyue_last_email<br/>上次登录邮箱"]
        L8["xingyue_member_no<br/>默认显示名的编号"]
        L9["xingyue_uid<br/>云端分配的全局唯一编号"]
        L10["xingyue_sync_key<br/>是否把秘钥同步到云端"]
        L11["xingyue_auth<br/>登录凭证"]
        L12["xingyue_last_user<br/>上次登录的账号 id（识别换账号）"]
        L13["xingyue_panel<br/>⚠️ 面板状态（全仓库零使用，死常量）"]
        L14["浏览器缓存<br/>本地导入的 Live2D 模型"]
    end

    subgraph 云端 Supabase
        S0["auth.users<br/>登录账号"]
        S1["profiles<br/>uid / 昵称 / 头像 / 签名"]
        S2["user_settings<br/>API 提供商 + 我的角色（秘钥可选加密存）"]
        S3["chat_sessions<br/>对话记录（按角色分组）"]
        S4["uid_pool<br/>编号池（客户端完全读不到）"]
    end

    subgraph 托管
        S5["GitHub Pages<br/>代码 / 模型 / 图片"]
    end

    L1 --> CHAT["聊天时使用"]
    L2 --> CHAT
    L3 --> CHAT
    L4 --> CHAT
    L11 --> AUTH["身份识别"]

    style S0 fill:#ffe8cc
    style S4 fill:#ffe8cc
```

**当前状态（这些键都在 `assets/js/store.js` 的 `KEYS` 里定义，共 13 个）**：

| 数据 | 存哪 | 换设备能带走吗 |
| --- | --- | --- |
| 昵称 / 头像 / 签名 | ☁️ 云端 + 本地 | ✅ |
| uid（「小窝第 N 成员」）| ☁️ 云端（`uid_pool`）+ 本地缓存 | ✅ |
| 我的角色 / API 提供商设置 | ☁️ 云端 + 本地 | ✅ |
| 对话记录 | ☁️ 云端 + 本地 | ✅ |
| **API 秘钥** | 默认 📴 本地；勾选后 ☁️ 加密上云 | 看勾没勾 |
| 登录凭证 / 偏好 / 界面状态 | 📴 只在本地 | ❌ |

> 细节见 `docs/07-云端同步.md`。

---

## 六、流程里的「可改动点」（大改前先看这个）

| # | 现状 | 可以怎么改 |
| --- | --- | --- |
| 1 | 登录页和首页挤在一个 `index.html`，靠 `display` 切换 | 拆成两个页面，或用前端路由（hash 切换），代码更好维护 |
| 2 | **游客和登录用户能做的事几乎一样**（登录只是显示名字） | 加权限分层：未登录只能试聊 N 句 / 不能改角色 / 有广告位… |
| 3 | 老用户下次打开**跳过登录页**（visited 标记） | 改成"必须登录"，或"每次都显示登录页但记住账号" |
| 4 | ~~聊天记录只在本地~~ | ✅ **已做**：登录后同步到 Supabase，多设备同一份对话 |
| 5 | ~~用户资料（昵称/头像）只在本地~~ | ✅ **已做**：绑定到账号，换设备也能带过去 |
| 6 | ~~访客没有账号概念~~ | ✅ **已做**：注册是独立的一张卡（邮箱 + 密码 + 确认密码 + 验证码），不再和登录共用 OTP |
| 7 | 侧边栏菜单硬编码在 `index.html` | 改成数据驱动（像 `common.js` 那样），以后加页面只改一处 |
| 8 | 子页面之间是普通跳转，没有统一框架 | 保持轻量（零构建），但可以抽公共模板 |
| 9 | 只有一个"官方角色 + 我的角色"概念 | 可以扩展：角色商店 / 分享角色 / 导入别人的角色卡 |
| 10 | 聊天是纯文字 | 可以加语音（TTS 朗读 + 唇形同步，Live2D 已预留 LipSync 分组）|

---

## 七、相关文档

- `docs/01-网站定位.md`：这个站是做什么的
- `docs/02-网站结构.md`：目录与文件职责
- `docs/03-Git协作指南.md`：环境准备 + 日常协同 + 冲突处理
- `docs/04-AI协作约定.md`：人 + AI 协作的分工与指令模板
- `docs/05-网站流程图.md`：本文（现状流程图 + 可改动点）
- `docs/06-测试账号.md`：测试账号
- `docs/07-云端同步.md`：数据库表、uid 规则、隐私设计
- `docs/08-踩坑与错误记录.md`：踩坑记录 + 已知问题清单 + 提交前自查表
- `AGENTS.md`：仓库级约定（AI 每次会话自动读）

---

## 八、改造进度

> 2026-09 确认的改造方向，**下面标记 ✅ 的已经上线**。

### 8.1 登录 / 注册（✅ 已完成）

进入网站**直接显示登录卡**（不再有"选择方式"那一张），三张卡之间互相跳转：

```mermaid
flowchart TD
    A["进站 → 登录卡"] --> B["方式一：邮箱 + 密码 → [登录]"]
    A --> B2["方式二：邮箱 + [发送] + 验证码 → [登录]"]
    A --> B3["[忘记密码？] → 找回密码卡"]
    A --> C["[没有账号？去注册一个吧] → 注册卡"]
    A --> G["[先随便逛逛] → 游客进首页"]

    C --> C1["[邮箱] + [密码] + [再次输入密码] + [验证码] + [发送] → [注册]"]
    B3 --> B4["[邮箱] + [验证码] + [发送] → [新密码] + [再次输入] → [重置密码]"]
    B4 --> A

    B --> H["进首页（凭证存本地，定时续期）"]
    B2 --> H
    C1 --> H
```

要点（都已实现）：
- 登录卡**同时支持密码和验证码**（卡内用「改用验证码登录 / 改用密码登录」切换）
- 密码规则：**至少 8 位且含字母和数字**，输入时实时校验；不符合才提示、符合则整行隐藏并把输入框描绿
- 每个密码框右侧有**小眼睛**（线条图标、无悬停效果、点击切换明文/密文）
- 注册卡 = 邮箱 + 密码 + 确认密码 + 验证码（验证码与「发送」按钮同一行、等高）
- **找回密码**：邮箱验证码 → 新密码（重置成功自动回登录卡并填好邮箱）
- 一张卡里「改用验证码登录 ｜ 忘记密码？」并排一行
- 技术：Supabase `signUp` / `signInWithPassword` / `verifyOtp` / `recover` / 修改密码（REST，零依赖）

### 8.2 个人中心（✅ 已完成）

```mermaid
flowchart TD
    U["个人中心 user.html"] --> S1["① 个人资料<br/>头像（上传 / 恢复默认）· 昵称 · 签名"]
    U --> S2["② API 设置<br/>提供商卡片 · 切换 · 编辑 · 测试 · 拉模型列表"]
    U --> S3["③ 角色设定<br/>我的角色增删改 · 模型配置 · 导入本地模型"]
    U --> S4["④ 对话记录<br/>记忆对数 · 清空 · 历史列表"]
    U --> S5["底部：退出登录（原地确认）"]
```

要点（都已实现）：
- `settings.html` **已删除**，设置全部并入个人中心（四个分区 + 左侧分区导航）
- 侧边栏的「设置」菜单项**已删除**，入口只有**左下角用户区**
- 角色面板里的「+ 添加人物」→ 跳到个人中心的角色分区（`user.html#role`）
- 页面数：11 → 10（删掉 `settings.html`）→ **又是 11**（新增 `zidongyulan.html` 自动预览页）
- 个人页面以后还会按站长的设计图再改（当前是临时版本）

### 8.3 连带影响

| 原来 | 改成 |
| --- | --- |
| 侧边栏「设置」→ settings.html | 删除该项；左下角用户区 → 个人中心 |
| settings.html（三视图）| 并入 user.html（四分区）|
| 登录只有验证码 | 密码 / 验证码双方式 + 忘记密码 |
| 角色面板「+ 添加人物」→ settings.html#role | → 个人中心角色分区 |
