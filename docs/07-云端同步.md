# 云端同步（个人页面的数据跟着账号走）

> 目标：换设备 / 换浏览器登录后，**昵称、头像、签名、角色、对话记录都还在**。
> 做法：用 Supabase 的数据库（免费额度足够），配合行级安全保证「每个人只能看自己的」。

---

## 一、你要做的一步：执行数据库脚本

1. 打开 Supabase 后台 → 左侧 **SQL Editor** → **New query**
2. 打开本仓库的 **`tools/supabase-schema.sql`**，把**全文**复制粘贴进去
3. 点 **Run**
4. 看到 Success 就完成了 ✅（脚本可以重复执行，不会报错）

> 脚本会建 4 张表 + 2 个触发器 + 行级安全策略 + **表级权限（GRANT）**，详见文件里的注释。
>
> ⚠️ **如果执行后仍报错 `permission denied for table xxx (42501)`**，说明表级权限没给上：
> 单独把脚本第 ⑧ 段（`grant ... to authenticated;` 那几行）再执行一次即可。
> 原因：RLS 策略管"行"，`GRANT` 管"表"，新版 Supabase 不会自动授予表权限。

---

## 二、数据分别存在哪

| 数据 | 存哪 | 换设备能带走吗 |
| --- | --- | --- |
| 昵称 / 头像 / 签名 | ☁️ 云端 + 本地 | ✅ |
| **uid**（「小窝第 N 成员」的编号）| ☁️ 云端（`uid_pool`）| ✅ |
| 我的角色（含模型配置）| ☁️ 云端 + 本地 | ✅ |
| 对话记录 | ☁️ 云端 + 本地 | ✅ |
| **API 秘钥（Key）** | 📴 **只在本地** | ❌ 换设备要重新填 |
| 登录凭证 | 📴 只在本地 | —— |

**为什么 API 秘钥不上云**：它是"能花钱的东西"，一旦上云就等于存在别人服务器上。
上云的是"设置结构"（用哪个服务商、模型名），**Key 那一项留本地**。
> 以后如果你想要"秘钥也同步"，跟我说，我改成加密后上传（安全性会下降，需要你确认）。

---

## 三、uid 是怎么分配的（对应「小窝第 N 成员」）

```
uid 池：1, 2, 3, ... 2000        ← 预先生成，记录每个编号被谁占用
        │
新用户注册 ──▶ 触发器随机挑一个「空闲」的 uid
        │
用户注销账号 ──▶ 该 uid 还回池子（变回空闲）
        │
下一个新用户 ──▶ 可能就拿到这个空出来的编号 ✅
```

- **保证不重复**：uid 在数据库里是主键，天然唯一
- **默认名字**：用户没设昵称时，显示成 `小窝第 12 成员`（12 就是他的 uid）
- **注销后回收**：不是"永久占用"，编号会流转给后来的人

---

## 四、隐私与安全设计

| 措施 | 说明 |
| --- | --- |
| **行级安全（RLS）** | 三张数据表都开了：每个人**只能 select/insert/update 自己那一行**，前端就算被改也读不到别人的数据 |
| **uid_pool 完全封闭** | 不给客户端任何策略，只有数据库触发器（security definer）能改 |
| **前端只带 Publishable key** | 这是公开的、受限的钥匙；Secret key 永远不碰前端 |
| **级联删除** | 注销账号时，资料 / 设置 / 对话记录一起删（数据库层保证）|
| **秘钥不上云** | API Key 只存浏览器本地 |

---

## 五、会新增的前端功能

| 功能 | 说明 |
| --- | --- |
| 登录后拉取云端资料 | 登录成功 → 用 uid 显示「小窝第 N 成员」，昵称/头像/签名用云端的 |
| 保存时同时写云端 | 点「保存」→ 本地 + 云端各写一份（云端失败不影响本地）|
| 角色 / 对话同步 | 切角色、聊天后自动同步（防抖，不要每次都请求）|
| **注销账号** | 个人资料页加一个危险按钮（二次确认）→ 删除账号 + 清空本地 + 释放 uid |

---

## 六、相关文件

- `tools/supabase-schema.sql`：数据库脚本（在 Supabase 里执行这个）
- `assets/data/supabase-config.js`：Supabase 配置（URL + Publishable key）
- `assets/js/auth.js`：登录模块（后续会加云端读写的接口）
- `docs/06-测试账号.md`：测试账号
- `docs/05-网站流程图.md`：页面与登录流程
