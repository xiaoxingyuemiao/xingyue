# 网站结构（Site Map）

## 0.0.1网站结构

### 首页
#### 顶部导航栏
    PS：从上到下  依次对应  从左到右

图标
关于（展开）
    星瑶
    月瓷
    小喵
插画
动态壁纸
漫画
动画
活动
动态
周边
登录（登录过后直接显示用户头像，没有就为默认头像）

#### 内容
大动态插画（暂定）

#### 底部导航栏
版权信息，各个相关网站链接等等

## 当前实现进度

> 最后校准：2026-09-21（按代码逐条核对过；全站共 **11 个页面**）

- ✅ 首页（登录注册 / 游客进入，无任何开场动画，白底）
- ✅ 左侧导航栏（纯白，默认收起为 44px 窄条 + 网站图标，鼠标移入自动展开、移出 0.1s 收回；
     展开 90px，菜单按钮 44px 高；菜单：首页 / 插画 / 周边 / 动态 / 关于（hover 子菜单：星瑶月瓷小喵）；
     **左下角用户区只显示头像**（名字已去掉，hover 也不变色），点它进个人中心）
- ✅ 顶部导航栏（各子页面：只保留 插画 / 周边 / 动态 / 关于 下拉，样式与主页统一）
- ✅ 关于页 ×3（带角色立绘展示 + "还在装修"徽章）
- ✅ 底部导航栏（版权信息、相关网站链接）
- ✅ 首页聊天界面（右侧窄栏与输入框对齐，气泡动画 + 三点等待，隐藏滚动条；
     状态记忆，每次打开自动接续上次对话；**对话历史本地 + 云端各一份**）
- ✅ 个人中心（`user.html`：四个分区 —— ① 个人资料 ② API 设置 ③ 角色设定 ④ 对话记录，
     左侧分区导航；头像上传、昵称（**上限 12 字**）、签名、退出登录、注销账号）
- ✅ 登录 / 注册 / 找回密码（Supabase 邮箱，零依赖 REST）：进站**直接是登录卡**，
     卡内可切「密码登录 / 验证码登录」；注册是**独立的一张卡**（邮箱 + 密码 + 确认密码 + 验证码）；
     忘记密码 = 邮箱验证码 → 新密码；凭证存本地并定时续期
- ✅ 云端同步（`profiles` / `user_settings` / `chat_sessions` 三张表 + RLS；uid 决定默认名
     「小窝第 N 成员」；API 秘钥可选加密上云 —— 详见 `docs/07-云端同步.md`）
- ✅ Live2D 角色（首页舞台已接入 default 模型：聊天情绪 → 表情 / 动作，含窗口与角色切换；
     星瑶 / 月瓷 专属模型待补，未配置 model 时统一回落 default）
- ✅ 插画画廊（`chahua.html`：16 个画框，9:16 / 1:1 / 16:9 / 20:9 各 4 个，
     布局用**固定种子**（不再每次刷新随机）；已挂 `images/` 里 6 张图，其余留木色占位）
- ✅ 自动预览页（`zidongyulan.html`：临时幻灯片，`images/` 全部图片循环播放，17 种切换效果）
- ✅ 周边页（`zhoubian.html`：深色主题 + 粒子背景；粒子代码已抽成 `assets/js/mesh-bg.js`）
- ⬜ 插画 / 动态壁纸 / 漫画 / 动画 / 直播 / 活动 / 周边**正式内容**

> ⚠️ **`settings.html` 已删除**，设置全部并入个人中心。
> 旧文档里凡是提到「设置页 / 用户设置页」的，现在都指 **个人中心（`user.html`）**。
