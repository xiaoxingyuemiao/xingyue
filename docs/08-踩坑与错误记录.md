# 踩坑与错误记录

> **用途**：把踩过的坑、报过的错、修法、以及「下次怎么避免」都记在这里，防止重复踩。
> **谁能改**：⚠️ 本文件是 `docs/` 目录里**唯一允许写入的文件**（白名单）。
> `docs/` 其它文件一律**只读**，要改先问站长。
> **最后更新**：2026-09-21

---

## 一、怎么记（规则）

1. **当天的坑当天记**，别攒着 —— 隔一天就只剩「好像报过什么错」。
2. 每条必须写清 **四件事**，缺一条这条记录就白写：
   | 项 | 说明 |
   | --- | --- |
   | **现象** | 用户/浏览器看到的是什么（报错原文、界面表现），不要写「不工作」 |
   | **根因** | 真正的原因（不是「那次改了 XX 就好了」） |
   | **修法** | 改了哪个文件哪一行 |
   | **怎么避免** | 下次在什么情况下要想起这条（写成可执行的检查动作） |
3. **新的坑追加在对应分类末尾**，编号一旦用过就不再复用（方便在别处引用「见坑 #07」）。
4. 记「错误」也记「失败方案」——试过但走不通的路同样是坑，能省下以后重复试错的时间。
5. 修没修好要写清状态：`✅ 已修` / `🟡 部分修` / `⬜ 未修（已知问题）`。

**复制这段当模板：**

```markdown
### #编号 一句话标题（P0/P1/P2）

- **日期**：YYYY-MM-DD
- **模块 / 文件**：`xxx.html`、`assets/js/xxx.js:123`
- **现象**：
- **根因**：
- **修法**：
- **怎么避免**：
- **状态**：✅ 已修
```

---

## 二、2026-09-21 这一轮（插画页「进入自动预览」+ 两个 bug 修复）

### #01 点「➤」不发送消息，点角色按钮反而把消息发出去了（P1）

- **日期**：2026-09-21
- **模块 / 文件**：`index.html`、`assets/js/main.js`（出问题的写法在原第 19 行，改好后在第 21 行）
- **现象**：点 ➤ 完全没反应；点输入框左边的角色按钮，面板打开的同时**输入框里的字被直接发了出去**。回车发送正常，所以很久没被发现。
- **根因**：`const sendButton = document.querySelector(".chat-input button")`。
  `.chat-input` 里的 DOM 顺序是 `角色按钮 → input → 发送按钮`，而 `querySelector` 返回的是**文档顺序第一个**匹配项 —— 拿到的是角色按钮。发送监听器于是绑在角色按钮上。
  （角色按钮自己的 `stopPropagation()` 挡不住**同一元素上**的其它监听器。）
- **修法**：发送按钮加 `id="chat-send"`，改成 `document.querySelector("#chat-send")`。
- **怎么避免**：**按钮一律按 id 取**。看到 `querySelector("某个容器 button")` 这种写法就要警觉：它取的是「第一个」，不是「我脑子里那个」。同一容器里有多个同类元素时，用 id 或 `:last-child`。
- **状态**：✅ 已修（已用无头浏览器核对 DOM 顺序与绑定）

### #02 本地导入的 Live2D 模型必然失败 —— Service Worker 的三层连环坑（P0）

- **日期**：2026-09-21
- **模块 / 文件**：`assets/js/live2d-custom.js`、`l2d-sw.js`（原 `assets/js/l2d-sw.js`）
- **现象**：个人中心「导入本地模型」提示成功，但角色模型加载 404；修到一半又变成界面**永远卡在「正在导入模型文件……」**。
- **根因**：三层问题叠在一起，**少修一层反而更糟**：
  1. **scope 越界**：`register("assets/js/l2d-sw.js", { scope: "/" })`。SW 的作用域不能超出**脚本所在目录**（默认上限就是 `/assets/js/`），请求 `/` 直接抛 `SecurityError`，被 `catch` 吞掉只留一条 console 警告。要用 `Service-Worker-Allowed` 响应头才能放宽，而 **GitHub Pages 发不了自定义响应头**。
  2. **SW 只能拦截「它控制的页面」发出的请求**：把 SW 挪到 `assets/`、scope 改成 `assets/` 之后，注册成功、脚本 200，但虚拟路径**仍然 404** —— 因为 `index.html` / `user.html` 在站点根目录，不在 `assets/` 作用域内，页面发出的请求根本不经过 SW。
  3. **`navigator.serviceWorker.ready` 会永久挂住**：它等的是「**作用域覆盖当前页面**」的 SW。前两层修好后，注册成功了，但 ready 永不 resolve → `importFolder()` 永不返回 → 导入界面卡死。（旧代码因为第 1 层直接抛错、跳过了 ready，所以当时的表象反而是「提示成功」。）
- **修法**：
  - `l2d-sw.js` **移到站点根目录**（和 `index.html` / `user.html` 同级），注册 `siteBase() + "l2d-sw.js"`、scope `siteBase()`；
  - 新增 `waitActivated(reg)` 取代 `navigator.serviceWorker.ready`：等**这个注册**的 `installing/waiting` worker 变成 `activated`，并留 8 秒兜底，避免界面卡死。
- **怎么避免**：**动 Service Worker 时同时看三件事 —— ①脚本放在哪（决定作用域上限）②作用域盖不盖得住「被拦截的请求所在的页面」③等待激活用的是 ready 还是 statechange。** 另外：凡是用 `catch` 吞掉 SW 注册错误的地方，都要把错误**显示出来**，否则用户看到的是「成功」。
- **状态**：✅ 已修（无头浏览器端到端实测：注册 scope=站点根、页面 `controller` 有值、虚拟路径 `200` 并从缓存读回内容，服务器侧收不到该请求）

### #03 验证 SW 时自己踩的坑（方法论，下次省时间）

- **日期**：2026-09-21
- **现象与根因**（都属于「工具/验证手段」而非产品代码）：
  | 坑 | 现象 | 怎么处理 |
  | --- | --- | --- |
  | **`--virtual-time-budget` 快进定时器** | 页面里的 `setTimeout` 超时先触发，真实异步（SW 注册/缓存）还没完成 → 误判成 `TIMEOUT` | 测 SW、缓存、网络这类**真实异步**时不要用虚拟时间；改用「真实时间 + `Start-Process` 启动、Sleep 后 Kill」 |
  | **`--dump-dom` 无输出**（这版 Edge） | stdout/stderr 都是 0 字节，看起来像没跑 | 改用 `--screenshot` 截图，或让页面把结果 `fetch` 回报给本地服务器，从服务器日志读时间线 |
  | **`window.close()` 会取消在途请求** | 最后几条日志上报丢失，误以为步骤没执行 | 收尾不要立刻 close，先 `setTimeout(..., 2000)` 留出发送时间 |
  | **PowerShell 里 `node -e` 多行脚本** | 引号被 PowerShell 吃掉，报 `SyntaxError: Unexpected end of input` | 写成 `.js` 文件再跑，或用单引号包裹、避免 `\"` 转义 |
  | **沙箱受限令牌下浏览器起不来** | Edge 报 `mojo platform_channel Check failed`、crashpad `OpenProcess 拒绝访问`（受限令牌创建不了命名管道） | 这是**沙箱预期行为**，不是系统坏了。渲染类验证需要**一次性提权**（`danger-full-access`）；提权后 `--screenshot` 实测可用 |
- **怎么避免**：凡是「异步 + 环境受限」的验证，**先设计好观察手段**再动手（回报日志 / 截图 / 落盘），不要在结论上猜。

### #04 把「每次随机」改成「固定布局」时，漏了最后一处 `Math.random()`（P2）

- **日期**：2026-09-21
- **模块 / 文件**：`assets/js/gallery.js`
- **现象**：画廊改成固定布局后，画框的比例、位置、间距都固定了，但**背景花草每次刷新仍在变**。
- **根因**：随机源已统一封装成 `random()`（固定种子），但 `buildDeco()` 里判断「这个格子要不要空着」时直接写了 `Math.random() < CELL_SKIP`，绕过了封装 —— 只改了封装函数，没搜全调用点。
- **修法**：改成 `random() < CELL_SKIP`。
- **怎么避免**：**把随机改成确定性时，第一件事就是 `grep Math.random` 把每一处列出来**，改完再搜一遍确认只剩注释里的提及。封装函数只挡得住走封装的那部分调用，绕过的那几处最容易被漏掉。
- **状态**：✅ 已修（用无头浏览器加载同一页面两次比对：16 个画框的比例 / 位置 / 尺寸 + 127 个花草完全一致）

### #05 CSS 3D 翻折「往哪边倒」不能凭感觉写正负号（P2）

- **日期**：2026-09-21
- **模块 / 文件**：`assets/js/auto-preview.js`、`assets/css/auto-preview.css`
- **现象**：自动预览的八向翻折里，有的方向「向后翻」、有的「向前翻」、还有的几乎看不出在动（三种状况混在一起）。
- **根因**：两处错：
  1. 「进入」和「离开」用了**互为相反**的旋转角（`in: rotateX(-90deg)` / `out: rotateX(90deg)`）—— 一边朝屏幕后方倒，另一边必然朝观众倒，方向当然不统一；
  2. 对角方向（左上/右上/左下/右下）的**旋转轴和「折哪条边」没对上**，元素绕了错误的轴，转起来像没动。
- **修法**：每个方向只留**一个**「向后」角度 `back`，进入用它 → 0、离开用 0 → 它（两边转向自然一致）；对角方向的轴改成垂直于对应那条对角线。另外把旧图的淡出改成淡到**全透明**，并让旧图压在新图上面，消掉两图叠着看的「重合」感。
- **怎么避免**：CSS 3D 里 **Z 轴指向观众**，「向后」= 朝 −Z 倒。**不要猜符号**：拿元素四个角做一次罗德里格斯旋转，看四角平均 z 是正还是负（负 = 向后）就定下来了 —— 这次就是这么定出八个方向的。
- **状态**：✅ 已修（数值验证：8 方向 × 进入/离开 = 16 项，z 全为负，且每组的进入与离开 z 完全相同）

---

## 三、历史坑汇总（整理自两份「新的东西」与 `07-云端同步.md`）

> 原始记录仍在 `docs/小喵  新的东西.md` 第八节、`docs/拾雨  新的东西.md` 第三/七/八节，这里只做汇总索引。

### 3.1 前端 / 浏览器

| # | 坑 | 表现 | 怎么处理 |
| --- | --- | --- | --- |
| H01 | **CSS 同优先级靠后者生效** | `.mp-scrollbar` 与 `.mp-window.flipped .mp-scrollbar` 同为 0,2,0，翻转规则写在前面 → 一直失效 | 覆盖规则必须写在默认规则**之后** |
| H02 | **绝对定位元素不设 width 会塌** | 包含块只有 46px 宽，面板被压成 46px，文字逐字换行变竖排 | 显式给 `width`；注意别同时设 `left` 和 `right`（会被夹死） |
| H03 | **flex「两个方向」都要管** | 页脚跑到屏幕中间：只给 `main` 加了 `display: flex`，但它自己不长大 | `main` 加 `flex: 1`（管自己作为子项长大）+ `display: flex`（管内部能分配）；`footer` 加 `margin-top: auto` 兜底 |
| H04 | **修完布局把内容挤到正中** | `main` 撑高后，`justify-content: center` 把内容挪到屏幕中间 | 改 `flex-start` + `padding-top: 15vh` |
| H05 | **流场里两个点会黏在一起** | 流场只跟位置有关，两点走到同一处被同一股力推着，看似只有一个点 | 给每个点的流场加相位偏移（按 id 偏移时间参数） |
| H06 | **`CELL` 用绝对像素，小屏反而更慢** | 桌面 1920 切成 40 格、手机 390 只切 8 格，粒子密度高 12 倍 → 手机 9fps | 做手机适配时 `N` 和 `CELL` 都要随屏幕尺寸降 |
| H07 | **`clip-path: path()` 方案失败** | 音乐功能那一版没能做出来，最后全删 | 重做前先想清楚渲染方案，别直接上手写 |

> ⚠️ 渲染类问题**命令测不了**：要么真实浏览器截图，要么「把数据打印到页面上让人念数字」（`main`/`footer` 的 top/bottom/height 直接印出来）。

### 3.2 Git / 沙箱 / 工具链

| # | 坑 | 表现 | 怎么处理 |
| --- | --- | --- | --- |
| H08 | **PowerShell 写提交信息带 BOM** | `Set-Content -Encoding UTF8` 会加 `EF BB BF`，git 原样收进提交对象 | 用 `[System.IO.File]::WriteAllText($p,$t,(New-Object System.Text.UTF8Encoding($false)))`，并检查前 3 字节 |
| H09 | **编辑工具会丢文件 BOM** | 改 `index.html` 后开头的 `EF BB BF` 没了，「完全还原」变成多余 diff | 补回 BOM，用 `git diff` 确认零差异 |
| H10 | **沙箱里 git 推不上去** | schannel 报 `SEC_E_NO_CREDENTIALS`；换 OpenSSL 后卡在 credential helper（git 强制 `sh -c`，受限令牌起不了 `sh`） | 推送需要提权；SSH 同样走不通（`ssh.exe` 一样报 signal pipe） |
| H11 | **后台任务与前台命令的 `$env:TEMP` 不是同一个目录** | 后台脚本报 `MODULE_NOT_FOUND` | 后台命令里用绝对路径，或改用 `node -e` 内联 |
| H12 | **`npx` 装不了包** | `npm error EPERM`（沙箱不许写 `~/.npm-cache`），`npx.ps1` 还被执行策略拦 | 不依赖第三方包，自己写最小 DOM 桩做测试 |
| H13 | **无头浏览器起不来** | Edge `--headless` 报 `mojo platform_channel Check failed`、crashpad `OpenProcess 拒绝访问` | 受限令牌创建不了命名管道 —— 属预期行为，不是环境坏了；见 #03 的提权方案 |

### 3.3 Supabase / 账号

| # | 坑 | 表现 | 怎么处理 |
| --- | --- | --- | --- |
| H14 | **有 RLS 没 GRANT** | `42501 permission denied for table profiles` | 建表后除了 RLS 策略，还要 `grant ... to authenticated` |
| H15 | **建表前创建的账号没有 profiles 行** | 登录报 `23502 null value in column "uid"` | 执行「补齐历史用户」SQL，给老账号补 uid |
| H16 | **前端不能直接删自己账号** | 注销 405 | 改用数据库函数 `delete_own_account()` |
| H17 | **换账号看到上一个账号的资料** | 本地 localStorage 残留 + 兜底合并 | 登录时按 `user_id` 判断，换账号先清本地 |
| H18 | **验证码位数按错** | 按 6 位限制，实际是 8 位，输入被吃掉 | 放宽到 4~10 位；位数取决于 Supabase 项目设置，别写死 |
| H19 | **假邮箱永远收不到码** | `114514@qq.com` 这类编的地址，测试必然卡在「发送验证码」，与 SMTP 配置无关 | 测试用真实可收信的邮箱 |
| H20 | **项目没开 OTP 注册** | `422 Signups not allowed for otp`；邮箱没确认过的账号也发不出验证码 | 后台手动建号一定勾 `Auto Confirm User` |

---

## 四、已知问题清单（2026-09-21 全仓库巡检）

> 这些问题**已在代码/文档里确认存在**。修完请把状态改成 ✅ 并注明日期。
> **4.1 的文档不一致已在 2026-09-21 修掉一批**（站长批准后校准，详见下表）；4.2 起的代码问题还没动手。

### 4.1 文档与代码不一致（会误导后来的人）

**✅ 2026-09-21 已修正（站长批准后校准）**：

| 位置 | 原来错在哪 | 现在 |
| --- | --- | --- |
| `docs/02-网站结构.md` | 侧边栏菜单还写「设置」、聊天还写「设置页可调 / 历史存本地」、还叫「用户设置页」、缺画廊与自动预览页 | ✅ 整节重写，按代码逐条核对 |
| `docs/05-网站流程图.md` | 页面地图画着 `settings.html`（同文件别处却写「已删除」，自相矛盾）；说「还没有找回密码」（已实现）；说「聊天只在本地」（已云端）；初始化顺序漏 `cloud.js`；localStorage 只登记 8 个键（实际 **13 个**）；相关文档重复列了两次 `docs/03` | ✅ 全部改正，页面地图补 `zidongyulan.html` |
| `docs/07-云端同步.md` | 说「秘钥不上云」、「uid 池 1~2000 / 随机挑」、「auth.js 后续会加云端接口」 | ✅ 改为「默认不上云、勾选后 AES-GCM 加密上云」、「1000~3000 **顺序**分配，1~999 保留」，并补 RPC 注销与换账号清残留 |
| `docs/06-测试账号.md` | 仍挂「注销账号还没测」，与 `docs/拾雨  新的东西.md:60` 的实测结果冲突 | ✅ 改成「已实测正常」，并写明**假邮箱测不了注销**这个真原因 |
| `docs/04-AI协作约定.md:80`、`docs/03-Git协作指南.md:427,441` | 分工表/举例里仍列已删除的 `settings.html` | ✅ 改成 `user.html` / `zhoubian.html` |

> ⚠️ **`docs/06` 那次冲突的教训**：一份文档说「没测」、另一份说「测过了」时，
> **先找做过的人确认，再改文档** —— 不要照着旧文档直接下结论（这次差点把已完成的注销功能记成待办）。

**4.1 现在没有未修项了**（唯一剩下的一条其实不算问题）：

> `docs/拾雨  第二次更改  2026-09-21 22-16.md:132` 的「还没做的」里列的过时项已修完；
> 工作报告按规矩不回头改，知道即可。

**✅ 2026-09-21 后续也修掉了**：

| 位置 | 修法 |
| --- | --- |
| `tools/make-flow-excalidraw.js` 会产出 `settings.html` 节点 | 节点表更新到现状（三张登录卡 / 个人中心四分区 / `zidongyulan.html` / 13 个键 / 云端四张表）；**并且发现它会覆盖站长手改的流程图**，已加输出路径保护（见 §4.2） |
| `tools/supabase-schema.sql:10` 注释写「uid 池（1~2000）」 | 改成「1000~3000 正常分配区，1~999 保留区」 |
| `AGENTS.md:32` 举例「文档里还在写已删除的 `settings.html`」 | 改成「2026-09-21 刚校准过一批，但文档永远可能再次落后」 |

> ✅ **`README.md` 已在 2026-09-21 同步完毕，不再是问题**：补登记 `cloud.js`、`zidongyulan.html`、`assets/css/auto-preview.css`、`AGENTS.md`、`README.md` 自身和全部 CSS 文件名；脚本加载顺序补齐到实际 11 个脚本并加上 `user.html` 的顺序；`l2d-sw.js` 路径改到站点根目录；「本地开发」补了 `npm run hooks`。
> **核对方式（以后改完 README 可以照做）**：把 README 里所有含目录的路径逐个 `Test-Path`，再把仓库里每个文件反向核对是否登记 —— 双向都通过才算同步。

### 4.2 代码里的已知缺陷

> 行号是 **2026-09-21 记录时**的位置。代码动过之后行号会漂，找不到就按表里的**关键字**搜索（例如 `hero-`、`CACHE_NAME`、`lp-footer`）。

| 位置 | 问题 | 影响 |
| --- | --- | --- |
| `assets/css/style.css:352-530,680-705` | 21 个 class（`hero-*` / `news-*` / `character-*` / `download-*`）全站从未使用 | 约 5KB 死样式 |
| `assets/css/style.css:579` | `.about-card` 在 236 行之外**又定义了一次** | 维护时容易改错那一条 |
| `live2d.html` / `live2d-guide.html` 页脚 | 自建 `lp-footer`：无「联系站长」、「赞助」≠「友情赞助」、「不要点」≠「不要点！」 | 与 `common.js` 文案不一致（可能是有意做成独立子站，需确认） |
| `index.html` / `user.html` | 没有页脚与版权行（未用 `common.js`） | 版权信息不统一 |

**✅ 2026-09-21 已修（站长选「只修不影响页面长相的那批」）**：

| 原问题 | 修法 |
| --- | --- |
| `main.js` 气泡文案让用户「去『设置』页面」（入口已删） | 改成「去**个人中心**的 API 设置里」/「去**个人中心**检查一下 API 配置」 |
| `settings.js` 用 `innerHTML` 拼 `error.message`（含用户填的 baseUrl） | 改 `createElement` + `textContent`，不再走 HTML 解析 |
| `store.js` 的 `panel` 死常量 | 删掉该键（已确认全仓库零引用） |
| `auth.js` 硬编码 `LS_KEY = "xingyue_auth"` 与 `store.js` 重复 | 改为 `window.Store.KEYS.auth`（两个页面都先加载 store.js，安全） |
| `CACHE_NAME` 在 `live2d-custom.js` 与 `l2d-sw.js` 双写 | SW 是独立上下文、无法共享常量，所以**保留双写但加锁**：两处都写「必须同步改」，并在 `tools/check.js` 新增第 7 项校验，不一致直接报错 |
| `main.js` / `user.js` 同名 `renderAvatar` | `user.js` 的改名 `renderUserAvatar`（含 1 处调用），并注明原因 |
| `main.js` 的续期 `setInterval` 从不清理 | 存下 id，`pagehide` 时清理（进 bfcache 时不清，回来还要继续续期）；并防重复启动 |
| `user.js` 的注销倒计时 | 原本已在「重开 / 走完」时清理，再补 `pagehide` 清理 |
| `tools/make-flow-excalidraw.js` 会把 `settings.html` 画回流程图 | 见下方「⚠️ 顺带发现的更严重的坑」 |

> ⚠️ **顺带发现的更严重的坑（已修）**：`tools/make-flow-excalidraw.js` 原本 `writeFileSync` 到
> `docs/网站流程图.excalidraw` —— 那是**站长在 excalidraw.com 上手改过的那份**，
> 重跑脚本会**整文件覆盖、且无法恢复**（`docs/08` 只登记了「会画回 settings.html」，没意识到覆盖风险）。
> 修法：① 默认输出改到 `docs/网站流程图-生成版.excalidraw`，**永不碰手改版**；
> ② 想显式覆盖手改版必须加 `--force`，否则直接拒绝并退出；
> ③ 支持 `node tools/make-flow-excalidraw.js 目标路径` 指定别处输出；
> ④ 顺手把节点表更新到现状（三张登录卡、个人中心四分区、`zidongyulan.html`、13 个 localStorage 键、云端四张表）。
> 验证：输出到临时路径跑通（66 节点 / 51 箭头 / 自检通过），并确认手改版 252618 字节未变、临时文件已删。

### 4.3 体积与性能

| 位置 | 问题 | 数字 |
| --- | --- | --- |
| `assets/live2d/default/ARGNori.4096/` | 首页一进来就把模型全部拉下来，目录名暗示 4096 贴图 | 3 张贴图 9.07MB + moc3 1.41MB ≈ **10.5MB**；可降 2048 或转 WebP |
| `images/xingyao-bg.jpg` == `images/xingyao-avatar.jpg` | SHA256 完全相同的两份（一份做全屏轮播、一份做 20px 头像） | 白占 106KB；头像应另生成小图 |
| `images/yueci-bg.jpg` == `images/yueci-avatar.jpg` | 同上 | 白占 142KB |
| `images/zhushitu.jpg`（801KB）、`supingbeijing.jpg`（660KB） | 首页/手机背景大图 | 见 `home.css:768,1270` |
| `assets/vendor/oh-my-live2d.min.js` | 唯一第三方依赖（已 vendor 化） | 981KB |

### 4.4 安全 / 隐私

| 位置 | 问题 | 建议 |
| --- | --- | --- |
| `docs/06-测试账号.md:12-13,23` | **明文测试账密**，而站点地址是公开的 | 仓库转公开前必须删掉口令行 |
| `assets/js/cloud.js:273-283` | 云端秘钥加密的 salt 硬编码、密钥由 `user_id + email` 派生 | 属设计取舍（注释已说明「主要靠 RLS」），需确认是否接受 |

---

## 五、提交前自查清单（防再犯）

```
□ 点按钮/取元素，用的是 id 还是宽泛的类选择器？（见 #01）
□ 动了 Service Worker？脚本位置 / scope / 等激活方式，三样都看了吗？（见 #02）
□ 动了 flex 布局？flex:1（自己长大）和 display:flex（内部能分配）都加了吗？（见 H03）
□ 报错被 catch 吞掉了吗？用户看到的是「成功」还是真的成功？（见 #02）
□ 新增/移动了文件？grep 一下别处有没有引用它（含 README、docs、tools）
□ 新增了页面？#site-header / #site-footer 占位、PAGE 标识、node tools/check.js 都过了吗？
□ 提交信息有没有 BOM？（见 H08）
□ 渲染类改动验证过吗？真实浏览器截图，或把数据打出来看（别只看属性「有没有生效」）
□ 文档里的路径与事实还对得上吗？对不上就在本文件登记一条
```

---

## 六、相关文档

- `docs/03-Git协作指南.md`：环境准备、协同、冲突处理
- `docs/04-AI协作约定.md`：人 + AI 协作的分工与铁律
- `docs/05-网站流程图.md`：现状流程图 + 改造进度
- `docs/07-云端同步.md`：数据库表结构、uid 规则、隐私设计
- `docs/小喵  新的东西.md`、`docs/拾雨  新的东西.md`：两位各自的阶段记录（坑的原始出处）
- `docs/08-踩坑与错误记录.md`：本文
